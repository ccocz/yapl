{
	is_prime(n): {
	  ok = true;
	  if (n % 2 == 0):
	    ok = false;
	  i = 3;
	  while (i * i <= n): {
	    if (n % i == 0): {
	      ok = false;
	    }
	    i = i + 2;
	  }
	  return ok;
	}

	ans = is_prime(10);
}
