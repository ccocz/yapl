{
	is_prime(n): {
	  ok = true;
	  if (n % 2 == 0):
	    ok = false;
	  i = 3;
	  while (i * i <= n): {
	    out(i);
	    if (n % i == 0): {
	      ok = false;
	    }
	    i = i + 2;
	  }
	  return ok;
	}

	ans = is_prime(100);

	if (ans == true):
	    out("prime");
	else:
	    out("not prime");
}
